<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ClinicaVeterinaraMobile.VetPage"
             Title="Lista Medici">

    <StackLayout Margin="20" Spacing="15">
        <Button Text="+ Adăugare medic"
                Clicked="OnAddVetClicked"
                BackgroundColor="#FF69B4" 
                TextColor="White"
                FontSize="20"
                FontAttributes="Bold"
                CornerRadius="25"
                HeightRequest="60"
                HorizontalOptions="Fill" />
        <ListView x:Name="listView" 
                  HasUnevenRows="True"
                  SeparatorVisibility="None"
                  ItemSelected="OnListViewItemSelected">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Frame Margin="0,5" 
                               Padding="15" 
                               CornerRadius="12" 
                               HasShadow="True"
                               BorderColor="#F0F0F0">
                            <HorizontalStackLayout Spacing="15">
                                <Label Text="👩‍⚕️" FontSize="30" VerticalOptions="Center" />

                                <VerticalStackLayout VerticalOptions="Center">
                                    <Label Text="{Binding VetName}" 
                                           FontSize="18" 
                                           FontAttributes="Bold" 
                                           TextColor="Pink" />
                                    <Label Text="{Binding Specialization}" 
                                           FontSize="14" 
                                           TextColor="Gray" />
                                </VerticalStackLayout>
                            </HorizontalStackLayout>
                        </Frame>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>